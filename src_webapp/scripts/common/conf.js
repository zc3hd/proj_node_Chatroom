(function(win, $) {
  var conf = win.conf = win.conf || {
    // =============================
    // 本地测试跨域测试
    //开启跨域模式
    api_key:true,
    api_port:1011,
    // 请求的根路径
    root_path:'',
  };
})(window, jQuery);
